// importando a biblioteca padrão de entrada e saida de dados
// std (standard)  i(input) o(output)
#include <stdio.h>
// Inicío do programa 
int main(){ 
// Comando para exibir o texto na tela 
// o comando printf só executa porque você fez a importação
// da biblioteca stdio.
printf("Hello World\n");

// o comando abaixo informa ao compilador que programa acabou
return 0;

}